apiVersion: consul.hashicorp.com/v1alpha1
kind: ServiceRouter
metadata:
  name: user
spec:
  routes:
    - match:
        http:
          pathPrefix: /user
      destination:
        service: wshoto-demo-user
